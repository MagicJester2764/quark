[package]
name = "console"
version = "0.1.0"
edition = "2021"

[dependencies]
libquark = { path = "../libquark" }
font8x8 = { version = "0.3", default-features = false, features = ["unicode"] }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
